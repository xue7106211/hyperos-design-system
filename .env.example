# TinaCMS — local filesystem mode (default for development)
TINA_PUBLIC_IS_LOCAL=true

# TinaCloud (optional — for cloud editing without self-hosted backend)
# NEXT_PUBLIC_TINA_CLIENT_ID=
# TINA_TOKEN=

# Self-hosted production auth (Phase 2+ — when not using local mode)
# NEXTAUTH_SECRET=
